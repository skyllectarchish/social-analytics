fastapi[standard]>=0.136.0
uvicorn[standard]>=0.34.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0
pydantic[email]>=2.0
pydantic-settings>=2.0
clickhouse-connect>=0.8.0
clickhouse-migrations>=0.4.0
cryptography>=44.0.0
httpx>=0.28.0          # HTTP client for Meta (Instagram) and Google (YouTube) API calls
python-multipart>=0.0.18
python-dotenv>=1.0.0

# --- Tier 2 / F4 — optional batch-job dependencies ---
# Only required to run app/jobs/sentiment_batch.py and topic_clustering.py.
# The API itself starts without them.
anthropic>=0.40.0
scikit-learn>=1.4.0

# --- Tier 4 — Ollama Cloud client for the AI Copilot features ---
# Used by app/ai/client.py to talk to https://ollama.com. The Tier 2
# sentiment + topic_clustering batch jobs still use the anthropic SDK
# above — the two are independent.
ollama>=0.4.0

# --- Tier 2 — in-process scheduler for batch jobs ---
# Drives competitor_sync (daily), sentiment_batch (hourly), and
# topic_clustering (weekly) via main.py's FastAPI lifespan. Can be disabled
# by setting ENABLE_SCHEDULER=false (e.g., when running cron externally).
apscheduler>=3.10.0
